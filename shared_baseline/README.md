# Shared Baseline

This folder contains the common reproduced Lu et al. (2025) U-Net baseline used consistently across Sequential, Grid, and Hybrid comparisons.

This shared baseline is intentionally compact and keeps the original reference files only: model implementation, configuration, evaluation protocol, and summary metrics. The three search modes reference this same baseline rather than carrying separate baseline packages.
